# HomeRent Backend

All setup and run instructions are documented in the root `README.md`.

From this folder, use the commands listed in the root guide (for example `npm install`, `npm run db:init`, `npm run dev`).
